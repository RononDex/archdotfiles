1. Set keyboard layouts in
/etc/X11/xorg.conf.d/20-keyboard.conf

Section "InputClass"
    Identifier "keyboard"
    MatchIsKeyboard "yes"
    Option "XkbLayout" "ch"
    Option "XkbVariant" "de"
EndSection


~/.xinitrc:
setxkbmap ch -variant de

2. zsh
install zsh
install zsh-completions
chsh -s full-path-to-shell
install oh-my-zsh
sh -c "$(wget https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"

3. Network manager
pacman -S networkmanager
systemctl enable NetworkManager
pacman -S gnome-keyring (for wifi wpa keyring)

4. dotfiles
mkdir github
cd github 
git clone https://github.com/RononDex/archdotfiles
cp -R ~/github/archdotfiles ~/

4. Window manager
i3-gaps
polybar
lxterminal
xsession
light-git (AUR)
flameshot (AUR)
compton
arc-gtk theme (AUR)
lxappearance
nerd-fonts-complete (AUR)
...

5. Display manager
lightdm
webkit greeter
litarvan (theme)

5. lock screen
light-locker


